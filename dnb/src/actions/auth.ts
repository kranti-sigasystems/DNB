'use server';

// Legacy auth actions - use auth.actions.ts instead
export async function login(email: string, password: string) {
  // Placeholder - use auth.actions.ts
  return { success: false };
}
